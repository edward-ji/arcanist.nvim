# Compiled parser -- platform/arch-specific, built locally by `make`.
/parser/*.so

# Help tags -- regenerated per machine by the plugin manager on install.
/doc/tags

# Node tooling used only when regenerating tree-sitter-remarkup/src/*
# (grammar.js -> parser.c) during grammar development.
/tree-sitter-remarkup/node_modules/
/tree-sitter-remarkup/*.so
